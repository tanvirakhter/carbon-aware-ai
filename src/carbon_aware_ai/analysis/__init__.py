"""Analysis package (scaffolded).

The carbon-saving questions this project ultimately answers:

* :mod:`temporal`  - how much CO2 is avoided by shifting a workload to the
  greenest TIME within a region?
* :mod:`spatial`   - how much is avoided by moving it to the greenest REGION
  at a given time, and how wide is the spatial spread?
* :mod:`scheduler` - a forecast-driven scheduler vs a perfect-hindsight oracle:
  how much of the achievable saving does a realistic, forecast-only planner
  actually capture?

These modules are intentionally stubbed with TODOs; collection comes first so
data can accumulate before analysis is written.
"""
